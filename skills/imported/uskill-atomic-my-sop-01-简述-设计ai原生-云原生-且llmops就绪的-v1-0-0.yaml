skill_id: uskill-atomic-my-sop-01-简述-设计ai原生-云原生-且llmops就绪的-v1-0-0
meta:
  name: my_sop Step 1
  version: 1.0.0
  category: atomic
  domain: general
  description: '**简述**: 设计AI原生、云原生、且LLMOps就绪的系统架构，将AI能力升级为架构核心。'
  tags:
  - converted
  - sop
  - atomic
  - my-sop
  owner: uSkills
input:
  params:
  - name: context
    type: string
    required: false
    description: Execution context shared across the SOP chain.
    default: null
execute:
  prompt_template: 'You are executing an atomic SOP skill.

    SOP: my_sop

    Step objective: **简述**: 设计AI原生、云原生、且LLMOps就绪的系统架构，将AI能力升级为架构核心。

    Use {{context}} when provided and produce a structured result.'
  steps:
  - '**简述**: 设计AI原生、云原生、且LLMOps就绪的系统架构，将AI能力升级为架构核心。'
  temperature: 0.1
  model: gpt-5.4-mini
  tools: []
  fallback:
  - manual-review
output:
  format: json
  params:
  - status
  - result
  - notes
  validation_rules:
  - status must be one of success, partial, failed
  - result must summarize the step execution
control:
  timeout: 30
  retry: 1
  interrupt: true
  breakpoint_allowed: true
  mockable: true
source:
  sop_name: my_sop
  sop_path: /tmp/SOPs/my_sop.md
  step_index: 1
  raw_step: '**简述**: 设计AI原生、云原生、且LLMOps就绪的系统架构，将AI能力升级为架构核心。'
