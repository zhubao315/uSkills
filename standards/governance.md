# uSkill 治理规范

## 基本要求

1. 所有技能必须通过 `USkill` Schema 校验。
2. 每个技能必须保留 `source` 字段，确保 SOP 来源可追踪。
3. 技能必须声明 `control`，禁止无超时、无中断能力的黑盒执行。
4. 组合技能必须引用原子技能，不允许跳过基础资产层。

## 版本策略

- Patch：描述修正、兼容性增强
- Minor：新增字段或新增技能能力
- Major：Schema 破坏性变更

## 合并门禁

- `ruff check .`
- `pytest`
- 技能索引重新生成
- 文档可构建
